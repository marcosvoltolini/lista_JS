function transformacao() {
    let impar = 0
    let par = 0

    for (let i =0; i>0; i++);
     numeros.push(Number parseInt(prompt("Digite um número: ")));
    if (numeros % 2 == 0) {
        numeros = numeros + 1;
    }else{
        numeros = numeros + 1;
    }
    console.log ("Seu numero é:" numeros );
}